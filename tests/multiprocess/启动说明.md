# 🚀 测试启动说明

## 问题已修复 ✅

之前遇到的 `ModuleNotFoundError: No module named 'capswriter'` 问题已经解决！

现在可以正常启动测试了。

---

## 💡 推荐启动方式

### Windows 用户（最简单）

直接**双击运行**以下批处理文件：

```
tests/multiprocess/
├── start_server.bat         # 启动多进程 Server
├── start_monitor.bat        # 启动资源监控
├── start_test_client.bat    # 启动测试客户端
└── start_full_test.bat      # 一键完整测试
```

**完整测试流程**：

1. 双击 `start_full_test.bat`
2. 按照提示操作即可

**手动测试流程**（需要 3 个终端）：

1. 双击 `start_server.bat` （等待启动完成）
2. 双击 `start_monitor.bat` （观察资源）
3. 双击 `start_test_client.bat` （运行测试）

---

### Linux/Mac 用户

在终端中执行以下命令：

```bash
# 一键完整测试
./tests/multiprocess/start_full_test.sh

# 或手动测试（需要 3 个终端）
# 终端 1
./tests/multiprocess/start_server.sh

# 终端 2
./tests/multiprocess/start_monitor.sh

# 终端 3
./tests/multiprocess/start_test_client.sh
```

---

## 🔧 直接运行 Python 脚本

如果你更喜欢直接运行 Python，确保在**项目根目录**下执行：

```bash
# 一键测试
python tests/multiprocess/run_test.py

# 或手动测试
python tests/multiprocess/test_core_multiprocess.py    # 终端 1
python tests/multiprocess/monitor_resources.py         # 终端 2
python tests/multiprocess/test_concurrent_client.py    # 终端 3
```

---

## 📋 启动脚本功能说明

| 脚本 | 功能 | 说明 |
|------|------|------|
| `start_full_test` | 一键完整测试 | 交互式引导完成全部测试 |
| `start_server` | 启动多进程 Server | 启动 2 个识别进程 |
| `start_monitor` | 启动资源监控 | 实时显示 CPU/内存占用 |
| `start_test_client` | 启动测试客户端 | 发送测试文件并统计结果 |

---

## ⚙️ 启动脚本的优势

相比直接运行 Python 脚本：

✅ **自动设置编码**：解决中文乱码问题（Windows）
✅ **自动切换目录**：无需手动 cd 到项目根目录
✅ **显示提示信息**：清晰的启动提示
✅ **错误不自动关闭**：按任意键继续，便于查看错误

---

## 🎯 现在就开始测试！

### 最快捷的方式

**Windows**：
```
双击 tests\multiprocess\start_full_test.bat
```

**Linux/Mac**：
```bash
./tests/multiprocess/start_full_test.sh
```

---

## ⚠️ 注意事项

1. **依赖检查**：确保已安装 `psutil`
   ```bash
   pip install psutil
   ```

2. **测试文件**：确保 `ref_codes/samples/` 下有音频文件
   ```bash
   ls -lh ref_codes/samples/
   # 应该看到 01.mp3, 02.mp3
   ```

3. **端口占用**：确保端口 6016 未被占用

4. **内存要求**：建议 16GB+ 内存（最低 8GB）

---

## 🐛 故障排查

### 问题：启动脚本双击无反应（Windows）

**解决**：右键脚本 → "编辑"，检查内容是否正确，或在 CMD 中手动运行：
```bash
cd D:\MyFolders\Developments\0Python\20250814_CapsWriter-offline-with-AI\CapsWriter-Offline
tests\multiprocess\start_full_test.bat
```

### 问题：Linux/Mac 提示权限不足

**解决**：添加执行权限
```bash
chmod +x tests/multiprocess/*.sh
```

### 问题：找不到 capswriter 模块

**解决**：这个问题已经修复！确保使用最新的脚本。

如果仍有问题，检查是否在虚拟环境中：
```bash
# 激活虚拟环境
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

---

## 📖 更多文档

- **快速开始**：`QUICKSTART.md`
- **完整文档**：`README.md`
- **技术细节**：`OVERVIEW.md`
- **总体说明**：`测试方案总结.md`

---

**祝测试顺利！** 🎉
