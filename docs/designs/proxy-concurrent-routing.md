---
status: ACTIVE
---
# CEO Plan: ASR 代理并发路由与动态调度

Generated by /plan-ceo-review on 2026-06-30
Branch: master | Mode: SELECTIVE EXPANSION
Repo: zj1123581321/CapsWriter-Offline-with-AI

## Vision

### 10x Check
当前代理是"能用"级别——串行跑得通，但并发未验证，路由策略原始。10x 版本：代理成为一个算力感知、故障自愈的调度中心，消费方只需要配一个地址就能透明地利用所有设备，文件转录吞吐量随设备数量线性扩展，任何单点故障不影响整体进度。

### Delight Opportunities
1. 算力权重路由让 M1 Max 自然承担更多任务，不浪费高端硬件
2. RTF 动态反馈让路由随设备实际负载自适应
3. 后端健康恢复让临时故障不再需要重启代理
4. 消费方并发指南让下游开发者 10 分钟内改造完成
5. 并发测试脚本成为持久的回归测试资产

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | 并发测试脚本 | S | ACCEPTED (baseline) | 验证 v1 并发正确性是前提 |
| 2 | 设备算力权重路由 | S (~30 LOC) | ACCEPTED | M1 Max vs M2 Pro 算力差 2x，等量分配浪费 |
| 3 | 后端中途故障音频重发 | M (~150 LOC) | DEFERRED (Codex) | 协议语义未定义清楚，先做简单的路由改进 |
| 4 | RTF 加权路由 | S (~60 LOC) | ACCEPTED | 动态反馈补充静态权重的不足 |
| 5 | 消费方并发改造指南 | S (文档) | ACCEPTED | 消费方不并发，代理再智能也白搭 |
| 6 | 动态后端添加/移除 | M (~100 LOC) | DEFERRED | 2 台设备 pm2 restart 够用 |
| 7 | 代理层背压信号 | S | SKIPPED | 协议不支持，消费方并发是正确解法 |
| 8 | 后端健康恢复 (cooldown) | S (~15 LOC) | ACCEPTED (Codex) | v1 缺陷：unhealthy 后端永不恢复 |

## Accepted Scope (added to this plan)
- 写并发测试脚本，验证多任务同时路由到不同后端、结果正确回送、故障行为
- 设备算力权重路由：config_proxy.py 加 weight 字段，select_backend 改为 least-connections 策略
- **路由评分公式（2026-06-30 修正）**: `(active_tasks + 1) / weight + latency * 1e-6`。active_tasks 为主确保任务均匀分散，latency 仅作同等负载时的 tiebreaker。之前的公式 `(active_tasks+1) * latency / weight` 因 latency 方差过大（10-300x）导致所有任务堆积到延迟最低的单台后端。
- processing_latency 追踪：从 RecognitionMessage 的 time_submit/time_complete 计算（注意：不是真正的 RTF，因为缺少音频时长字段）。EWMA alpha=0.2，冷启动前 3 次不参与路由。异常值（<0 或 >300s）记 WARNING 并排除，同时重置 EWMA 避免僵尸惩罚。
- 后端健康恢复：unhealthy 后端 cooldown_seconds（默认 60s，可配置）后同时重置 consecutive_failures=0 和 healthy=True。全部后端 unhealthy 时，降级选 cooldown 中 active_tasks 最少的。
- weight > 0 启动时校验，防止除零崩溃
- 全功能可观测性日志（权重评分、RTF 统计、健康恢复事件）
- 消费方并发改造指南文档

## Deferred to TODOS.md
- 音频重发：Codex 指出协议语义复杂（task_id 重写、部分结果去重、内存管理），需更严谨设计后再做
- 动态后端添加/移除：当前 2 台设备，pm2 restart 够用。设备增到 5+ 台时再考虑

## Review Findings (from 11-section review)

### Architecture (D4)
- 100MB 音频缓存上限 → 现已 deferred，不再适用

### Error Map (D5)
- weight=0 配置防御 → 启动时校验 fail-fast

### Data Flow (D6)
- 重发后结果去重 → 随重发一起 deferred

### Observability (D7)
- 权重评分、RTF 统计、所有新功能的 INFO 级日志

### Codex Outside Voice
- 15 个发现，3 个改变了计划：
  1. RTF 数据源改为从 RecognitionMessage 提取（不是 last_result_time）
  2. 后端健康恢复机制（v1 缺陷修复）
  3. 音频重发 defer（协议复杂度高于预期）

## Implementation Tasks

- [ ] **T1 (P1, CC ~5min)** — proxy/backend — 加 weight 字段到 BackendState + config_proxy
  - Surfaced by: Scope decision #2
  - Files: `core/proxy/backend.py`, `config_proxy.py`
  - Verify: `python -m pytest tests/test_proxy_backend.py tests/test_proxy_router.py -q`

- [ ] **T2 (P1, CC ~5min)** — proxy/router — select_backend 改为 active_tasks/weight 评分
  - Surfaced by: Scope decision #2
  - Files: `core/proxy/router.py`
  - Verify: 新增测试验证权重路由行为

- [ ] **T3 (P1, CC ~10min)** — proxy/router — processing_latency 从 RecognitionMessage 提取并用于路由
  - Surfaced by: Codex: last_result_time 不是 RTF
  - Files: `core/proxy/router.py`, `core/proxy/backend.py`
  - 算法: EWMA alpha=0.2，冷启动前 3 次不参与路由评分。异常值 (<0 或 >60s) 记 WARNING 并排除。
  - Verify: 新增测试验证冷启动、正常反馈、异常值排除

- [ ] **T4 (P1, CC ~5min)** — proxy/backend — 健康恢复 cooldown 机制
  - Surfaced by: Codex: unhealthy 后端永不恢复
  - Files: `core/proxy/backend.py`, `config_proxy.py`
  - 语义: cooldown 后同时重置 consecutive_failures=0 和 healthy=True
  - 降级: 全部 unhealthy 时选 cooldown 中 active_tasks 最少的 + WARNING
  - Verify: 新增测试验证 cooldown 恢复 + 全部 unhealthy 降级

- [ ] **T5 (P1, CC ~5min)** — config_proxy — weight > 0 启动校验
  - Surfaced by: D5 (Error map)
  - Files: `core/proxy/proxy_server.py`
  - Verify: 测试 weight=0 时启动报错

- [ ] **T6 (P1, CC ~30min)** — scripts — 并发测试脚本（含 mock backend）
  - Surfaced by: Baseline scope
  - Files: `scripts/_verify_proxy_concurrent.py`
  - 注意: 需要 mock WS 服务器模拟多个 ASR 后端，或真实环境运行
  - Verify: 脚本能通过代理同时发 N 个任务到不同后端

- [ ] **T7 (P2, CC ~5min)** — proxy — 可观测性日志统一审查
  - Surfaced by: Section 8 (Observability): 新功能缺少日志
  - Files: `core/proxy/router.py`, `core/proxy/backend.py`
  - 注意: T2/T3/T4 各自内嵌基础日志，T7 做统一格式审查和补全
  - Verify: 日志包含权重评分、processing_latency 统计、cooldown 事件

- [ ] **T8 (P2, CC ~10min)** — docs — 消费方并发改造指南
  - Surfaced by: Scope decision #5
  - Files: `docs/消费方并发改造指南.md`
  - Verify: 文档包含示例代码和注意事项
